[pytest]
asyncio_default_fixture_loop_scope = function
markers =
    e2e: end-to-end tests that require playwright + a real uvicorn server
# e2e tests live in tests/e2e and are not collected by default.
# Run them with: pytest -m e2e tests/e2e
testpaths = tests
